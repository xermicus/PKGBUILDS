# Enable quotesay for bash
post_install() {
	echo "shuf -n1 ~/.quotes | cowsay" >> /etc/bash.bashrc
	echo "echo" >> /etc/bash.bashrc
}

post_upgrade() {
        echo "shuf -n1 ~/.quotes | cowsay" >> /etc/bash.bashrc
        echo "echo" >> /etc/bash.bashrc
}

